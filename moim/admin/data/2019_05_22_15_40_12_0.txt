select * from `club` where 'club_category' order by `club_hit' desc - 카테고리별 모임

select * from `club` order by 'club_hit' desc - 전체 모임

insert into `buy` value ('buy_num','buy_id', 'buy_club_num') - 결제완료시 구매테이블에 추가

update `club` set 'club_apply'='club_apply'+1 where `club_num`='club_num' - 결제완료시 신청인원 증가

update `club` set 'club_name'='$club_name','club_category'='$club_category','club_content'='$club_content','club_rent_info'='$club_rent_info',
'club_to'='$club_to','club_start'='$club_start','club_end'='$club_end','club_price'='$club_price','club_schedule'='$club_schedule','club_image'='$club_image','club_file'='$club_file' where club_num = '$club_num' - 개설된 모임 수정(관리자)

select * from buy Inner join club on buy.buy_num = club.club_num where club_name='모임이름'; - 구매테이블과 클럽테이블의 이너조인 (매출)

